{#
/**
 * @file
 * Lists group types available for adding groups.
 *
 * This list is displayed on the Add group admin page.
 *
 * Available variables:
 * - types: A list of group types, each with the following properties:
 *   - add_link: Link to create a group of this type.
 *   - description: Description of this group type.
 *
 * @see template_preprocess_group_add_list()
 *
 * @ingroup themeable
 */
#}
{% if types is not empty %}
  <dl>
    {% for type in types %}
      <dt>{{ type.add_link }}</dt>
      <dd>{{ type.description }}</dd>
    {% endfor %}
  </dl>
{% else %}
  <p>
    {% set create_group_type = path('entity.group_type.add_form') %}
    {% trans %}
    You have not created any group types yet. Go to the <a href="{{ create_group_type }}">group type creation page</a> to add a new group type.
    {% endtrans %}
  </p>
{% endif %}
